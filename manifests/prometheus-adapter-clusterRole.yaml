apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: ks-prometheus-adapter
rules:
- apiGroups:
  - ""
  resources:
  - nodes
  - namespaces
  - pods
  - services
  verbs:
  - get
  - list
  - watch
